import React from "react";
interface QueryProviderProps {
    children: React.ReactNode;
}
export declare const QueryClientProviderWrapper: ({ children, }: QueryProviderProps) => React.JSX.Element;
export {};
